// Dummy source file - validators are header-only for now
// Future: move implementations here if needed for complex logic

#include "pydantic_core/validators/mod.hpp"

namespace pydantic_core {
// Validator implementations are inline in headers
}